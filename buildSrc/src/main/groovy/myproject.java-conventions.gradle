plugins {
    id 'java'
}

group = 'com.builders'
version = '1.0.0'

repositories {
    mavenCentral()
}

dependencies {
    testImplementation "junit:junit:4.13"
}

project.delete(
        files("${rootDir}/outputGradle")
)